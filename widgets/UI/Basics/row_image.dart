import 'package:flutter/material.dart';
import 'package:flutter/src/foundation/key.dart';
import 'package:flutter/src/widgets/framework.dart';

class RowImagess extends StatelessWidget {
  const RowImagess({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        // body: RawImage(image: Image.network('https://picsum.photos/250?image=9')),
        );
  }
}
